name: Gates Workflows
on:
  pull_request:
    branches: [main]
jobs:
  run-all-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: exit 1
      - name: Run all tests.
        run: ./scripts/run-tests.sh

